// Ulises Cantor
// Period 5
// 10/9/2025
// Menchukov
// Computer Programming 3
// The program creates a display that starts with three buttons which change the text onscreen, 
// there is also a menu navigation bar at the top to switch between that screen and another saying that I am the best Java Coder

// Note to self:
// j frame is the back, the n there's a root pane, then a layered plane, then a content pane, then a glass pane
// content pane shows stuff, j stuff is outer thing
/*
import javax.swing.*;
import java.awt.*;
import java.awt.event.*;

public class Blackjack {
    /*
    // * Create the GUI and show it. For thread safety,
    // * this method should be invoked from the
    // * event-dispatching thread.
     
    private static void createAndShowGUI() {
        // Create and set up the window.
        JFrame frame = new JFrame("Blackjack");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.setLayout(new GridLayout(2, 3));

        // Add the ubiquitous "Hello World" label.
        JLabel label = new JLabel("Dealer Hidden");
        JButton button = new JButton("Hit");
        JLabel yoCard = new JLabel("Your Card");
        JLabel dealerCard = new JLabel("Dealer's Card");
        JButton button2 = new JButton("Stand");
        JLabel yoOthCard = new JLabel("Your Other Card");

        frame.add(label);
        frame.add(button);
        frame.add(yoCard);
        frame.add(dealerCard);
        frame.add(button2);
        frame.add(yoOthCard);

        frame.setVisible(true);

        frame.setSize(500,300);


    }

    public static void main(String[] args) {
        // Schedule a job for the event-dispatching thread:
        // creating and showing this application's GUI.
        javax.swing.SwingUtilities.invokeLater(new Runnable() {
            public void run() {
                createAndShowGUI();
            }
        });
    }
}
*/